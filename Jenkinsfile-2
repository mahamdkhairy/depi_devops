pipeline {
  agent { label 'slave' }
  environment {
   ANSIBLE_PRIVATE_KEY=credentials('secertfile') 
  }
  stages {
    stage('Hello') {
      steps {
        // sh 'ansible-galaxy collection install -r requirements.yml'
        sh 'ansible-playbook -i hosts --private-key=$ANSIBLE_PRIVATE_KEY playbook.yml'
      }
    }
  }
}
